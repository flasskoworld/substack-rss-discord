name: RSS to Discord

on:
  workflow_dispatch:
    inputs:
      mark_existing:
        description: "Mark current feed posts as seen without posting them"
        required: false
        default: false
        type: boolean
  schedule:
    - cron: "*/15 * * * *"

permissions:
  contents: write

jobs:
  post:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Check webhook secret
        if: ${{ !inputs.mark_existing }}
        env:
          DISCORD_WEBHOOK_URLS: ${{ secrets.DISCORD_WEBHOOK_URLS || secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ -z "$DISCORD_WEBHOOK_URLS" ]; then
            echo "Missing DISCORD_WEBHOOK_URLS repository secret."
            exit 1
          fi

      - name: Post new RSS items
        env:
          RSS_FEED_URL: https://streeteconomics.substack.com/feed
          DISCORD_WEBHOOK_URLS: ${{ secrets.DISCORD_WEBHOOK_URLS || secrets.DISCORD_WEBHOOK_URL }}
          RSS_STATE_PATH: .rss_state.json
          MAX_POSTS: "3"
          ALLOW_INSECURE_SSL: "1"
        run: |
          if [ "${{ inputs.mark_existing }}" = "true" ]; then
            echo "Marking existing RSS posts as seen..."
            python3 rss_to_discord.py --mark-existing
          else
            echo "Checking RSS feed for new posts..."
            python3 rss_to_discord.py
          fi

      - name: Save RSS state
        run: |
          if git diff --quiet .rss_state.json; then
            echo "No state changes."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add .rss_state.json
            git commit -m "Update RSS state"
            git push
          fi
